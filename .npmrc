# @shopify/app-bridge-react@3 declares a React <=18 peer range, but this app runs
# React 19. The combination is compatible in practice, so allow npm (including the
# `npm ci` used in CI/deploys) to resolve it instead of failing on ERESOLVE.
legacy-peer-deps=true
